# TYM 정비/진단 T-bot

TYM 트랙터 정비 및 진단 정보를 제공하는 챗봇입니다.

## 프로젝트 구조

```
C:\Users\Dongsama\
├── server.js          # Node.js 백엔드 서버 (실행 파일)
├── package.json       # 프로젝트 설정 및 의존성
├── public/            # 프론트엔드 파일
│   ├── index.html     # 메인 HTML 파일
│   ├── style.css      # 스타일시트
│   └── script.js      # 클라이언트 JavaScript
└── README.md          # 이 파일
```

## 실행 방법

### 1. 의존성 설치

먼저 Node.js가 설치되어 있어야 합니다. 그 다음 프로젝트 디렉토리에서 다음 명령어를 실행하세요:

```bash
cd C:\Users\Dongsama
npm install
```

### 2. 서버 실행

```bash
node server.js
```

또는

```bash
npm start
```

### 3. 브라우저에서 접속

서버가 실행되면 브라우저에서 다음 주소로 접속하세요:

```
http://localhost:3000
```

## 주요 기능

- **RAG 기반 검색**: 진단 데이터에서 관련 정보를 검색하여 제공
- **지능형 답변**: 질문에 맞는 내용만 추출하여 정교하게 답변
- **실시간 대화**: 대화형 인터페이스로 편리하게 사용
- **데이터 새로고침**: 진단 파일이 추가/수정되면 새로고침 가능

## 데이터 경로

### 로컬 실행 시
진단 데이터는 다음 경로 중 하나에 있어야 합니다:
- 프로젝트 내부: `diagnostics/` 폴더 (권장)
- 또는 환경 변수로 설정: `DIAGNOSTICS_PATH` 환경 변수

### 배포 시
프로젝트 루트의 `diagnostics/` 폴더에 진단 파일을 넣고 Git에 커밋하면 자동으로 배포됩니다.

이 경로의 모든 `.txt` 파일이 자동으로 로드됩니다.

## 사용 예시

- "T115 PTO 작동 안됨"
- "K110E DEF 필터 교체"
- "LX1100E 엔진 오류"
- "TX76 DPF 문제"

## 기술 스택

- **백엔드**: Node.js, Express
- **프론트엔드**: HTML, CSS, JavaScript
- **검색**: 키워드 기반 RAG (Retrieval-Augmented Generation)

## 🌐 외부 인터넷 배포 (무료)

이 프로젝트를 외부 인터넷에서 접속 가능하도록 무료로 배포할 수 있습니다!

### 빠른 시작
1. **배포가이드.md** 파일을 참조하여 Render.com에 배포하세요
2. 또는 **DEPLOYMENT.md** 파일에서 다양한 무료 배포 옵션을 확인하세요

### 주요 배포 플랫폼
- **Render.com** (권장) - 무료 티어 제공, 사용 간편
- **Railway** - 무료 크레딧 제공
- **Fly.io** - 무료 티어 제공

자세한 배포 방법은 `배포가이드.md` 또는 `DEPLOYMENT.md`를 참조하세요.


